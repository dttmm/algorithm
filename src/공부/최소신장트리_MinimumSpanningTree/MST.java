package 공부.최소신장트리_MinimumSpanningTree;

public class MST {
    // minimum spanning tree
    // 그래프에서 모든 노드를 연결할 때
    // 사용된 에지들의 가중치의 합을 최소로 하는 트리
    // 구현 방법에는 크루스칼, 프림이 있음

    // MST의 특징
    // 사이클을 포함하면 안됨
    // 사이클이 발생한다 -> 현재 에지보다 낮은 가중치들로 이미 연결되어 있다는 것을 의미
    // 노드가 N개 있으면, MST를 구성하는 에지의 개수는 N-1개임
}
