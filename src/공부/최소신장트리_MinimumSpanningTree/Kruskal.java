package 공부.최소신장트리_MinimumSpanningTree;

public class Kruskal {
    // 크루스칼로 구현하기

    // 초기화

    // 가중치가 낮은 에지부터 연결 시도

    // 사이클이 생기는지 판단하고
    // 사이클이 생기지 않으면 연결
    // 부모가 다르면 연결을 해도 사이클이 생기지 않음

    // 연결된 노드의 개수가 N-1이 될 때까지 반복하면서서
    // 가중치들의 합을 구하면 됨
}
